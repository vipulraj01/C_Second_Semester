#include<stdio.h>
int main()
{
    float b,h,aot;
    printf("enter the value of base and height = ");
    scanf("%f%f",&b,&h);
    aot=(float)1/(float)2*b*h;
    printf("area of triangle = %f",aot);
    return 0;
} 